package net.pureal.tests.traits

import org.spek.Spek
import net.pureal.traits.*

class Vector2Specs : Spek() {{
    given("some vectors") {
        val eX = vectorOf(1,0)
        val eY = vectorOf(0,1)

        on("") {it("") {
        }}
    }
}}